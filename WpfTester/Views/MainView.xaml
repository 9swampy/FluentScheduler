<Window x:Class="WpfTester.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:localViewModels="clr-namespace:WpfTester.ViewModels"
        xmlns:localViews="clr-namespace:WpfTester.Views"
        Title="MainWindow" Height="350" Width="525">
  <Window.Resources>
    <ResourceDictionary>
      <DataTemplate DataType="{x:Type localViewModels:TaskViewModel}">
        <localViews:TaskView />
      </DataTemplate>
    </ResourceDictionary>
  </Window.Resources>
  <Grid>    
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="1*" />
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
      <Button Content="Enable" Margin="5" Command="{Binding EnableCommand}" />
      <Button Content="Delay" Margin="5" Command="{Binding DelayCommand}" />
      <Button Content="Disable" Margin="5" Command="{Binding DisableCommand}" />
    </StackPanel>
    <ListView Grid.Row="1" ItemsSource="{Binding TaskList}">
      
    </ListView>
  </Grid>
</Window>
